### **多元复合函数求导法则（链式法则）——生动图解+实战案例**

---

#### **🔑 核心思想**
**链式法则**就像**快递配送网络**：
- **$z$ 是包裹**，要送到 **$x$ 或 $y$** 手中
- **中间站点**（$u,v$）会影响最终配送路径
- **每条路径的"运费"（导数）要相乘，不同路径的"运费"要相加**

---

### **📌 一、基础版（单变量链式法则）**
**情景**：$z = \ln(u)$，$u = x^2 + y$，求 $\frac{\partial z}{\partial x}$  
**变量关系图**：
```
[z] ←─ [u] ←─ [x]
           ↑
          [y]
```
**计算步骤**：
1. **路径唯一**：$z \leftarrow u \leftarrow x$  
2. **链式相乘**：$\frac{\partial z}{\partial x} = \frac{dz}{du} \cdot \frac{\partial u}{\partial x}$  
3. **具体计算**：
   - $\frac{dz}{du} = \frac{1}{u}$  
   - $\frac{\partial u}{\partial x} = 2x$  
4. **结果**：$\frac{\partial z}{\partial x} = \frac{2x}{x^2 + y}$  

---

### **🌳 二、树状多路径（经典题型）**
**情景**：$w = uv + \sin t$，其中 $u = e^x$, $v = x^2$, $t = xy$，求 $\frac{\partial w}{\partial x}$  
**变量关系图**：
```
        [w]
       / | \
     [u] [v] [t]
      |   |   |
     [x] [x] [x,y]
```
**计算步骤**：
1. **找出所有通向 $x$ 的路径**：  
   - 路径1：$w \leftarrow u \leftarrow x$  
   - 路径2：$w \leftarrow v \leftarrow x$  
   - 路径3：$w \leftarrow t \leftarrow x$  

2. **逐路径计算**：
   - 路径1：$\frac{\partial w}{\partial u} \cdot \frac{du}{dx} = v \cdot e^x$  
   - 路径2：$\frac{\partial w}{\partial v} \cdot \frac{dv}{dx} = u \cdot 2x$  
   - 路径3：$\frac{\partial w}{\partial t} \cdot \frac{\partial t}{\partial x} = \cos t \cdot y$  

3. **合并结果**：
   $$
   \frac{\partial w}{\partial x} = e^x \cdot x^2 + e^x \cdot 2x + \cos(xy) \cdot y
   $$

---

### **💡 四、常见误区警示**
| 错误类型 | 正确做法 | 实例对比 |
|---------|---------|---------|
| **漏路径** | 检查所有变量依赖 | 求 $\frac{\partial w}{\partial x}$ 时漏掉 $w \leftarrow t \leftarrow x$ |
| **混淆符号** | 明确偏导($\partial$)和全导($d$) | $\frac{dz}{du}$（单变量） vs $\frac{\partial z}{\partial u}$（多变量） |
| **未化简** | 合并同类项 | $e^x x^2 + 2x e^x = e^x(x^2 + 2x)$ |

---

### **🎯 六、终极记忆口诀**
> **"看分支，找路径；先相乘，后相加"**  
> 对应到快递网络：  
> - **看分支**：检查包裹有多少条配送路径  
> - **找路径**：确定经过哪些中转站  
> - **先相乘**：计算单条路径的"总运费"  
> - **后相加**：汇总所有路径的费用  

---

**📚 练习题**  
设 $z = \arctan(u/v)$，其中 $u = x+y$, $v = x-y$，尝试用链式法则求 $\frac{\partial z}{\partial x}$，并画出变量关系图。  
（答案可展开查看）  

<details>
<summary>点击查看答案</summary>

**变量关系图**：
```
        [z]
       /   \
     [u]   [v]
     / \   / \
   [x] [y][x][y]
```

**计算过程**：
$$
\frac{\partial z}{\partial x} = \frac{1}{1+(u/v)^2} \left( \frac{v \cdot 1 - u \cdot 1}{v^2} \right) + \frac{1}{1+(u/v)^2} \left( \frac{-u \cdot 1}{v^2} \right) = \frac{v - u}{v^2 + u^2}
$$
</details>