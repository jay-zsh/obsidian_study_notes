
# 28.5 练习

---

## 事实性练习

1. 关于归约，下列哪些陈述是正确的？
   - DAG-LPT 归约到 DAG-SPT
   - 3SAT 归约到独立集
   - N×N网格上的渗滤归约到N×N网格上的并查集
   - N×N网格上的并查集归约到N×N网格上的渗滤

问题1  
- DAG-LPT 归约到 DAG-SPT  
- 3SAT 归约到独立集  
- N×N网格上的渗滤归约到N×N网格上的并查集  
- N×N网格上的并查集归约到N×N网格上的渗滤  

---

## 程序性练习

1. 对下图使用课堂中的拓扑排序算法（所有平局按数字顺序解决：入度为零时取最小顶点，出边选择时取最小边），得到的顶点顺序是什么？  
   ![](PANG/99fab680b42e2b91567787c71ee747b6_MD5.jpg)  
   
2. 为边添加权重后，使用Dijkstra算法从顶点0寻找最短路径树(SPT)，第二个访问的顶点是哪个？  
   
3. 第2题的图是DAG（无环）。使用课堂中的DAG最短路径算法，顶点0之后将访问哪个顶点？  
   
4. 根据课堂中的DAG最短路径算法，首先访问顶点6。放松边(6→1)后，distTo[1]的值是多少？（假设0为起点）  
   
5. 从顶点0出发，上图的最长路径树包含多少条边？  

问题1  
DFS后序为 `3 2 4 5 1 0 6`，其逆序 `6 0 1 5 4 2 3` 即为拓扑排序结果  

问题2  
顶点2（距起点路径最短）  

问题3  
顶点1（拓扑排序中的下一顶点）  

问题4  
无穷大（distTo[6]为无穷大，故 distTo[6] + w(6,1) 仍为无穷大）  

问题5  
从0可达6个顶点，最长路径树含 V-1 = 5 条边