# 19.4 碰撞处理：线性探测与外部链化

在哈希技术中，当多个元素在数组中具有相同索引时即发生碰撞。

---

## Hug教授关于碰撞的专题讲解

哈希表处理碰撞的两种常用方法：

1. **线性探测：** 将碰撞键存储于数组其他位置，通常选择下一个可用空间。这种方法可见于分布式哈希表，后续计算机科学课程中将会涉及。
    
2. **外部链化：** 更直接的解决方案是将所有具有相同哈希值的键集中存储于专属容器（如`LinkedList`）。这种共享单一索引的条目集合被称为**桶**。