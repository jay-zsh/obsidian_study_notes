# 16.5 作为集合与映射的二叉搜索树
---

我们可以使用二叉搜索树来实现集合抽象数据类型。通过二叉搜索树结构，得益于其特有的排序特性（该特性使我们能够运用二分搜索策略），我们可以将`contains`方法的运行时间缩短至$\log(n)$对数级别。

---

通过将每个树节点存储`（键，值）`配对而非单一数值，我们同样可以将二叉搜索树转化为映射结构。系统将通过比对各元素的键值来确定其在树结构中的存储位置。