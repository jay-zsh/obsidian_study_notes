# 17.6  总结
---
- 二叉搜索树的最佳高度为$$Θ(\log N)$$，最差高度为$$Θ(N)$$
- 大O符号与最坏情况*并不等同*！
- B树是二叉搜索树的改良结构，能在最坏情况下为`add`和`contains`操作维持$$Θ(\log N)$$时间复杂度。其通过**插入时保持完美平衡**实现这一特性
- B树采用**节点可容纳值的数量上限LL**机制，而非传统二叉搜索树的单值节点结构
- B树的`add`操作会将新值直接插入到合适叶节点的有序位置。当节点达到容量上限时，执行**节点分裂**并将中间值向上层推送
