# 29.3 归并排序

我们过去已经介绍过归并排序，但作为提醒，该算法如下：

1. 将项目分成两半。
2. 对每一半进行归并排序。
3. 合并两个已排序的半部分以形成最终结果。

你可以在这里查看算法的演示。

归并排序的运行时间为 `Θ(NlogN)` — 我们不会在这里重新分析该算法，但如果你想提醒自己，可以参考此链接。

在合并步骤中使用的辅助数组需要 `Θ(N)` 的额外空间。注意，原地归并排序是可能的；然而，它非常复杂，并且运行时间会受到显著的常数因子影响，因此我们不会在这里讨论它。