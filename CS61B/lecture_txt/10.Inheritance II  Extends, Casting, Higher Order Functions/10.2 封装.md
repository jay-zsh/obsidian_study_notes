# 10.2 封装

---

### 复杂度

在构建大型程序时，我们最大的敌人是==复杂度==。

管理复杂度的有效工具：

- 层次化抽象
    - 创建抽象层次，并设立清晰的抽象屏障
    
- "为变化而设计"（David Parnas提出）
    - 以对象为中心组织程序
    - 让对象自主决定操作方式
    - 隐藏其他模块不需要的信息

对于大型项目，复杂度管理至关重要！

---

### 模块化与封装

**模块**：为实现特定任务或相关任务集而协同工作的方法集合。当模块的**实现细节完全隐藏**，仅通过规范化的接口进行访问时，我们称该模块已被**封装**。