# 14.1 引言

🚨 新数据结构警报 🚨：不相交集合

人们生活在各自的国家中。我们可以将每个国家视为一个集合，其所有公民作为集合中的元素。同一个人不能同时生活在两个不同国家。我们刚刚建模的正是**不相交集合**。

> 若两个集合无共同元素，则称为不相交集合。不相交集合（或称并查集）数据结构负责追踪固定数量的元素如何被划分为若干不相交集合。该数据结构包含两种操作：

1. `连接(x, y)`: 连接`x`和`y`，亦称`联合`
    
2. `是否连接(x, y)`: 若`x`和`y`连通（即同属一个集合）返回真
    

Hug教授对不相交集合的入门讲解

不相交集合数据结构包含固定数量的元素，每个元素初始位于独立子集。通过对某些元素`x`和`y`调用`连接(x, y)`，我们将子集合并。

例如，设有四个元素A、B、C、D。初始时每个元素自成集合：

![[6f8ef49642c0fe865ed4a142683cae69_MD5.jpg]]

{A} {B} {C} {D}

调用`连接(A, B)`后：

![[69e8ea7751fa0d607c3ca932d562aa79_MD5.jpg]]

{A, B} {C} {D}

注意A和B所在子集已被合并。以下`是否连接`调用结果：

`是否连接(A, B) -> 真`

`是否连接(A, C) -> 假`

调用`连接(A, D)`后：

![[6df843330612b371efe4cf0d8b57fdb4_MD5.jpg]]

{A, B, D} {C}

找到A所属集合并与D所属集合合并，形成A、B、D大集合。C保持独立。

`是否连接(A, D) -> 真` `是否连接(A, C) -> 假`

基于上述直观理解，我们正式定义不相交集合接口。请注意，**接口**规定了数据结构应具备的行为（而非实现方式）。因此，任何实现`DisjointSets`接口的类都必须包含以下函数：

```java
public interface DisjointSets {
    /** 连接两个元素p和q */
    void connect(int p, int q);

    /** 检查两个元素是否连通 */
    boolean isConnected(int p, int q); 
}
```

但如何存储数据以追踪元素所属集合？该使用何种数据结构来呈现这一精妙设计？本章不仅将学习如何实现迷人数据结构，更将见证数据结构实现的演化历程。我们将讨论四种设计方案直至满意：[_快速查找_](https://cs61b-2.gitbook.io/cs61b-textbook/14.-disjoint-sets/14.2-quick-find) _→_ [_快速联合_](https://cs61b-2.gitbook.io/cs61b-textbook/14.-disjoint-sets/14.3-quick-union) _→_ [_带权快速联合（WQU）_](https://cs61b-2.gitbook.io/cs61b-textbook/14.-disjoint-sets/14.4-weighted-quick-union-wqu) _→_ [_路径压缩WQU_](https://cs61b-2.gitbook.io/cs61b-textbook/14.-disjoint-sets/14.5-weighted-quick-union-with-path-compression)。**我们将见证设计决策如何显著影响渐进时间复杂度和代码复杂度。**